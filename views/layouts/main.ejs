<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle ? pageTitle + ' | ' : '' %><%= appName %></title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'><circle cx='16' cy='16' r='14' fill='%230677cc'/><text x='16' y='21' text-anchor='middle' font-size='14' fill='white'>AI</text></svg>">
  <%- include('../partials/headAssets') %>
</head>
<body>
  <header class="top-bar">
    <div class="top-bar__brand">
      <span class="logo" aria-hidden="true">◆</span>
      <span class="brand-name"><%= appName %></span>
    </div>
    <nav class="top-bar__nav">
      <% if (currentUser) { %>
        <span class="nav-item" aria-live="polite">Signed in as <strong><%= currentUser.email %></strong></span>
        <form class="nav-item" action="/logout" method="post">
          <button class="button button--ghost" type="submit">Sign out</button>
        </form>
      <% } else { %>
        <a class="button button--ghost" href="/login">Sign in</a>
      <% } %>
    </nav>
  </header>

  <main class="container container--with-sidebar" role="main">
    <div class="layout">
      <div class="layout__sidebar">
        <%- include('../partials/sidebar') %>
      </div>
      <div class="layout__content">
        <%- include('../partials/flash') %>
        <%- body %>
      </div>
    </div>
  </main>

  <footer class="footer">
    <p>© <%= new Date().getFullYear() %> ZenX · Built for rapid TQA assessments.</p>
  </footer>

  <%- include('../partials/scriptAssets') %>
</body>
</html>
